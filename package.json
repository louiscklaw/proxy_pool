{
  "name": "proxy_pool",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker_rebuild": "docker-compose rm -s -v -f && docker-compose up -d && docker-compose logs -f ",
    "gitUpdate": "git add . && git commit -m\"update proxy_pool,\" && git pull && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
